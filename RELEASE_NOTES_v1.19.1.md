# To-Do v1.19.1

本次版本更新应用视觉标识：

- 更换为新的蓝绿渐变勾选 App 图标
- 同步更新圆形 Launcher 图标
- 同步更新通知栏小图标与通知大图标
- 保留 To-Do / Note / Daily、图文 Note、Banner 四向手势、新手教程与情景教学等现有功能

> 建议直接覆盖安装，且不要先卸载旧版；在 applicationId 与签名一致的情况下，本地数据会继续保留。

## 下载与安装

在下方 **Assets** 中下载 `To-Do-v1.19.1.apk`，传到 Android 手机后打开安装。

本版本启用了新的正式发布签名。如果手机上已有其他签名构建的 To-Do，Android 可能提示无法覆盖安装；确认旧数据不需要保留后，卸载旧版再安装本版本即可。后续 GitHub Release 将继续使用本次签名，以支持正常覆盖升级。
